openapi: 3.0.1
info:
  title: Test API
  version: "1.0"
paths:
  /test:
    post:
      parameters:
        - in: query
          name: category
          schema:
            type: string
      responses:
        "200":
          description: OK
