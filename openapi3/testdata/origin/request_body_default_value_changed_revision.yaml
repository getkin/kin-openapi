openapi: 3.0.0
info:
  title: Sample API
  version: 1.0.0
paths:
  /products:
    post:
      summary: Create a new product
      operationId: createProduct
      requestBody:
        content:
          text/plain:
            schema:
              type: string
              default: "NewDefault"
        description: RequestBody   