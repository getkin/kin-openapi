openapi: 3.0.0
info:
  title: Arbitrary top-level key ref
  version: 1.0.0
paths:
  /users:
    get:
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "./arbitrary_key_schemas.yaml#/User"
