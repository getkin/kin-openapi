openapi: 3.0.1
info:
  title: Test API
  version: "2.0"
paths:
  /test:
    post:
      parameters:
        - in: query
          name: category
          schema:
            type: string
            maxLength: 15
      responses:
        "200":
          description: OK
